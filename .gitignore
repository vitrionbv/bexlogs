# OS / editors
.DS_Store
.idea/
.vscode/
.zed/
*.swp

# Environment
.env
.env.*.local
!.env.example
!.env.production.example

# MCP local tokens (never commit)
scripts/sentry-mcp.env
scripts/github-mcp.env

# Node
node_modules/
npm-debug.log
yarn-error.log
pnpm-debug.log

# Scraper
scraper/debug/
scraper/dist/
scraper/build/

# Local copies of remote debug artifacts (pulled with `compose cp`).
session_expired_artifacts/
session_expired_artifacts_tmp/

# Extension build output
extension/build/

# Deploy artefacts / local runtime
*.log
*.pid
tmp/
