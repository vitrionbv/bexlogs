# Copy to scripts/sentry-mcp.env (gitignored) and fill in values.
# User Auth Token from self-hosted Sentry (Settings → Auth Tokens).

SENTRY_BACKEND_ACCESS_TOKEN=sntrys_...   # sentry-backend.vitrion.dev
